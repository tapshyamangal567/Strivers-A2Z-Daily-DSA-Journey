// Problem : largest subarray with sum 0 

class Solution {
  public:
    int maxLen(vector<int>& arr) {
        int sum =0;
        int maxi =0;
        map<int , int> mpp;
        for(int i=0 ;i<nums.size();i++) {
            sum += nums[i];

            if(sum == 0) {
                maxi = i+1;
            }
            if(mp.find(sum) != mp.end()) {
                maxi = max(max , i-mp[sum]);
            }
            else {
                mp[sum] = i;
            }
        }
        return maxi;
    }
};
