// Problem : merge overlapping subintervals 

class Solution {
  public:
    vector<vector<int>> mergeOverlap(vector<vector<int>>& arr) {
        // Your code goes here
        sort(arr.begin(),arr.end());
        int n = arr.size();
        vector<vector<int>> ans;
        for(int i=0 ;i<n;i++) {
            if(ans.empty() || a[i][0] > ans.back()[1]) {
                ans.push_back{arr[i]};
            }
            else {
                ans.push_back()[1] = max(ans.back()[1] , arr[i][1]);
            }
        }
        return ans;
    }
};
