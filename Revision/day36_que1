class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        int n = nums.size();
        map<int , int>mpp;
        for(int i=0 ;i<n ;i++) {
            int sum = nums[i] + nums[j];
            int a = nums[0];
            int rem = sum - a;

             if(mpp.find(rem) != mpp.end()) {
                return {i,j};
             }
        }
        return {-1,-1};
    }
};
